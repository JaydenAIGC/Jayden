{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "python megastructure_web.py",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
